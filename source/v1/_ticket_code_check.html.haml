%h4#ticket_code_check <small>3.3</small> Check an attendee's ticket
.meta
  .method POST
  .url /event/attendees/code/:code/check.json

.description
  This endpoint allows you to mark the ticket as checked

  %h5 Available parameters
  %table.table.table-striped.well
    %col{width: '30%'}
    %col{width: '70%'}
    %thead
      %tr
        %th Parameter
        %th Description
    %tbody
      %tr
        %td at (optional)
        %td
          An ISO8601 encoded date to specify at which time the ticket was
          checked. The current time is used if left blank.
  .example
    = code(line_numbers: false) do
      :plain
        curl -i -H 'Authorization: event TOKEN' \
          -d 'at=2012-11-16T08:27:05-03:00' \
          https://api.welcu.com/v1/event/attendees/code/SC1F5E6/check.json

    %h5 Response
    = code(line_numbers: false) do
      :plain
        HTTP/1.1 202 Accepted
        Content-Type: application/json; charset=utf-8
        Cache-Control: no-cache
        Connection: close
