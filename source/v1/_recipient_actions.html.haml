%h4#list_invitations <small>4.3</small> Recipient actions
.meta
  .method POST
  .url /event/invitations/:invitation_id/recipients/:recipient_id/:action.json

.description
  Using this endpoint you can confirm or decline a recipient

  %h5 Available parameters
  %table.table.table-striped.well
    %col{width: '30%'}
    %col{width: '70%'}
    %thead
      %tr
        %th Parameter
        %th Description
    %tbody
      %tr
        %td :action
        %td
          The action to perform.
          The available actions are:
          %ul
            %li
              %strong confirm
              Confirm the recipient
            %li
              %strong unchecked
              Decline the recipient
      %tr
        %td :recipient_id
        %td The recipient's ID.
      %tr
        %td :invitation_id
        %td
          Filter by invitation. If you don't want to filter by invitation, use invitation_id = all.

  .example
    = code(line_numbers: false) do
      :plain
        curl -X POST -i -H 'Authorization: event TOKEN' \
          https://api.welcu.com/v1/event/invitations/1462/recipients/974534/decline.json

    %h5 Response
    = code('json') do
      :plain
        {
            "id": 974534,
            "invitation_id": 1462,
            "email": "recipient1@welcu.com",
            "first_name": "John",
            "last_name": "Doe",
            "state": "declined",
            "ticket_code": null,
            "ticket_url": null
        }
